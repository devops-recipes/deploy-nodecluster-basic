resources:
  - name: deploy-nodecluster-basic-cluster
    type: cluster
    integration: dr-nc

  - name: deploy-nodecluster-basic-appfile
    type: file
    pointer:
      sourceName: https://s3.amazonaws.com/devops.recipes.nodecluster.packages/deploy-nodecluster-basic-appfile.tar.gz # s3 bucket name
    seed:
      versionName: test # file name within the bucket
