resources:
  - name: deploy-clusternode-basic-cluster
    type: cluster
    integration: dr-nc

  - name: deploy-clusternode-basic-appfile
    type: file
    pointer:
      sourceName: depoy-clusternode-basic # s3 bucket name
    seed:
      versionName: test # file name within the bucket
