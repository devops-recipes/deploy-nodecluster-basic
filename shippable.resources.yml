resources:
  - name: deploy-nodecluster-basic-cluster
    type: cluster
    integration: dr-nc

  - name: deploy-nodecluster-basic-appfile
    type: file
    pointer:
      sourceName: devops.recipes.nodecluster.packages # s3 bucket name
    seed:
      versionName: test # file name within the bucket
